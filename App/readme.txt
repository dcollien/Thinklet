Thinklet App
============

Experimental thinklet desktop app built on Appjs (chromium + node.js). The good stuff is in './frontend/'. 'backend' only contains appjs configuration bits.

To build and launch app, open terminal in same directory as this readme and run './build.sh'